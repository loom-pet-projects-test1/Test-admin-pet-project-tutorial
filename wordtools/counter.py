"""Fill these in — every one of them currently returns the wrong answer."""


def count_words(text: str) -> int:
    return 0


def longest_word(text: str) -> str:
    return ""


def char_frequency(text: str) -> dict[str, int]:
    return {}
